
# Step-by-step Plan
1. Initialise Vite React TS project; add RTK, RRDv6, json-server.
2. Scaffold routes (Login, Register, Home, Profile, Share). Protected routing.
3. Implement Redux slices: auth (register/login/encrypt) and lists (CRUD).
4. Build UI components: Navbar, SearchSortBar, ListForm, ListItemCard.
5. Implement URL-driven search/sort and sync with state.
6. Add class components (Login, Register, Profile).
7. Add shareable read-only route: `/share/:listId`.
8. Add responsiveness and accessibility touches.
9. Seed `db.json` (empty). Manual tests.
10. Write README and planning docs. Push to GitHub with branches per brief.
